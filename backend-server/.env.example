# Google Sheets Configuration
# Get this from your Google Sheet URL
SPREADSHEET_ID=YOUR_SPREADSHEET_ID_HERE

# Server Configuration (for local development only)
PORT=3000

# CORS - Allow your extension to access the API
# In production, set this to chrome-extension://YOUR_EXTENSION_ID
ALLOWED_ORIGINS=*

# Service Account Credentials (for Vercel, paste entire JSON content)
# For local dev: Create .env file and paste full JSON or use file path
SERVICE_ACCOUNT_CREDENTIALS={"type":"service_account","project_id":"..."}
SERVICE_ACCOUNT_FILE=service-account-credentials.json
